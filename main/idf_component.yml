## IDF Component Manager Manifest File
dependencies:
  espressif/usb_host_hid: "^1.0.4"
  espressif/esp_lcd_lt8912b: "==0.2.0"
  espressif/esp_codec_dev: "^1.3.1"
  espressif/es8311:
    version: "^1.0.0"  
#  espressif/esp_hosted: ">=2.0.0"
    # wifi station test on ESP32-P4 (WiFi over an ESP32-C6 co-processor)
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp_hosted:
    version: "~2"
    rules:
      - if: "target in [esp32p4, esp32h2]"
